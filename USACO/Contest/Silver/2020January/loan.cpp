#include <algorithm>
#include <fstream>
#include <iostream>
#include <vector>
#include <map>
#include <set>
#include <queue>
#include <cmath>
#include <math.h>
using namespace std;
long long n,k,m;
bool cond1(int a){
    return ((float)(n)/(float)(x)*pow(((float)(x)-1)/(float)(x),(float))>=float(m))
}
int main(){
    ifstream fin("loan.in");
    ofstream fout("loan.out");
    fin>>n>>k>>m;

}
